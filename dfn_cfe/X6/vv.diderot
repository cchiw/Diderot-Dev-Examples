int outSize = 10;
vec2 x=[1,1];
vec2 y=[8,1];
vec2 exp = x*10;
ofield#2(2)[2] S = cfexp(exp,x);
ofield#2(2)[] D= S•y;

vec2 pos = [1,7];
strand sample (int ui, int vi) {

    //output vec2 out = inst(D, pos)*y;
    //output tensor[2] out =  ((pos*10)•y)*y;

    output tensor[2] out =  ((pos*10)•y)*y-inst(D, pos)*y;//Expected Solution:0

    update {
             stabilize;
    }
}

initially [ sample(ui, vi) | vi in 0..(outSize-1), ui in 0..(outSize-1) ];
