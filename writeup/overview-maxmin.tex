

\details{Maximal and Minimum between fields}{MaxF(), MinF()\\ \tab fty = field\#k($d$)[$\alpha$]  \\ \tab
fty $\times$ fty  $\rightarrow$ fty}{\branch{}}{none}{syntax ``Max()" instead of ``MaxF()}
{Examples}: Check out programs in {\exs{}/fn\_min-max}